/target
/result
/result-*
.direnv/

# Corpus, tokenized shards, tokenizer, checkpoints — all regenerable, all large.
/data/
/checkpoints/
/artifacts/

# Human review approvals — local-only state, not committed.
agent-orchestrator/approvals/
agent-orchestrator/reviews/

# MCP server build artifacts (source is committed, build output is not).
tools/loom/node_modules/
tools/loom/dist/
tools/loom/flake.lock
